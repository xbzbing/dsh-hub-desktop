# DSH Hub · 一期设计完整性走查报告

- **走查对象**:`design/dsh-hub-desktop.html`(高保真交互原型)+ `brand-spec.md`
- **对照基准**:`docs/PRD.md`(页面 §4 / 流程 §5 / 状态 §6 / 视觉 §7 / 文案 §8)
- **走查方式**:HTML 结构与脚本逐项核验 + 真实解析校验
- **结论**:✅ **一期设计完整度达标;G1/G2/G3 与 D1/D2 已全部决策——G1(骨架屏)与 G3(英文文案)纳入本期开发(T3 / T11),见 §3**

---

## 1. 结构校验(全过)

| 校验项 | 结果 |
|---|---|
| 内联脚本语法(`node --check`) | ✅ 通过(62KB 单脚本) |
| HTML 标签配对 | ✅ 无未闭合、无游离结束标签 |
| 占位符残留(`{{}}` / `[REPLACE]` / `.ph-img` / lorem / TODO) | ✅ 零残留 |
| 视图与浮层 `data-od-id` | ✅ 15 个,与交付说明一致,另有 `view-empty` 空态视图 |
| 交付物完整性 | ✅ HTML + brand-spec + 交付说明 + artifact.json 四件齐 |

## 2. PRD 覆盖矩阵

### §4 页面(9/9 全覆盖)

| PRD 章节 | 设计实现 | 结论 |
|---|---|---|
| 4.1 主界面/实例工作台 | `view-home`:统计卡 + 实例表格(名称/类型/状态/地址/版本/操作)+ 分组 + 侧边栏 + 空态 `view-empty` | ✅ 注:PRD 布局图为**列表行**,设计采用**表格**(信息密度更高,属正向差异,见 §3-D1) |
| 4.2 创建向导三步 | `wizard`:三种类型卡 → 本地(名称+高级:版本/数据目录/端口)/ SSH(主机别名/端口/用户名/认证方式 + **密钥预览** + 安全提示)/ 远程(URL + **自动识别登录**)→ Step3 确认卡(kv + 记住登录状态/记住密码,含安全等级说明) | ✅ 含密钥预览增强 |
| 4.3 实例详情 | `view-detail`:连接方式(地址/端口/隧道/密钥 + 未加密警告 + 复制)/ 认证(双因素/登录状态)/ 运行信息/ 审计记录 / **移除实例需输入名称确认**、主机指纹行 | ✅ |
| 4.4 实例工作区 | `view-workspace`:顶部工具条、会话 rail、内嵌 dsh 占位;状态浮层齐全:未连接(CTA)/ 连接中(进度线+加密通道)/ 失败(原因+重连)/ 断线横幅(自动重连第 N 次,不打断)/ 会话过期 | ✅ |
| 4.5 登录与二次验证面板 | `auth-panel`:密码 → 6 位分段验证码(aria-label 逐位)→ 8 位备份码 → 成功;失败统一提示 + **剩余机会计数**;`000000` 触发 5 次锁定 → **倒计时面板**(提交禁用,到点自动恢复);记住登录状态勾选;⌘D 快捷键 | ✅ 注:PRD §4.5 的"密码+验证码同屏一次提交"自适应路径**未实现**,走分步(见 §3-G2) |
| 4.6 首次改密引导 | `onboarding-change-password`:初始密码自动填入 + 新密码/确认 + 强度提示 + "稍后再说" | ✅ |
| 4.7 指纹确认 | `fingerprint-dialog`:**首次**(信息态,指纹可核对 + "不再询问"默认不勾选)与**变更**(红色警示态:可能中间人 + 建议先与管理核对)双变体 | ✅ |
| 4.8 设置页 | `view-settings`:通用(外观三态/语言/**托盘/自启/系统通知**开关)/ 数据(数据目录+打开/清除登录/清除实例,危险项红色) / 关于(版本/检查更新/审计日志入口) | ✅ |
| 4.9 系统托盘 | `tray-menu`:运行中实例子菜单(状态点+打开)、无实例空文案;通知开关已入设置 | ✅ |

### §5 核心流程(4/4 有演示触发)

| 流程 | 演示触发 | 结论 |
|---|---|---|
| 5.1 本地创建→连接 | 向导 → 连接约 1.15s 进工作区 | ✅ |
| 5.2 SSH 断线→自动恢复 | 「服务器 · 主力」连接后 4.2s 横幅 → 2.6s 恢复;「GPU 实验机」演示指纹 | ✅ |
| 5.3 远程 + 2FA 全旅程 | 密码→验证码→备份码→错误(000000)→锁定倒计时;「版本尝鲜」9s 后会话过期浮层 | ✅ |
| 5.4 登出与清除 | 详情移除需输名称;设置页清除登录/清除实例 | ✅ |

### §6 状态与反馈

| PRD 项 | 设计实现 | 结论 |
|---|---|---|
| 六态状态机 | 七种状态(idle/connecting/auth/interrupted/connected/error/**locked**),状态点+文案+图标三重编码,类型徽标线性图标区分 | ✅ locked 从 PRD 的"需登录子态"提为独立第七态且用红色,更警示,合理(§3-D2) |
| 反馈分级 | toast(43 处使用)/ 横幅(reconnect)/ 浮层 / 对话框 / 危险操作红色确认 | ✅ |
| 空态 / 加载态 / 错误态 | 空态(插画+CTA)✅;连接中转圈+进度线 ✅;**列表骨架屏 ✗**(§3-G1);错误态(原因+重试)✅ | ⚠️ |

### §7 视觉(与 brand-spec 一致)

| PRD 项 | 设计实现 | 结论 |
|---|---|---|
| 明暗双主题 | OKLch 六个基础 token,明暗两套同名,实时切换(跟随系统/浅/深) | ✅ |
| accent 克制 | 仅主按钮/选中指示/开关/焦点环四处;每屏 ≤2 次 | ✅ |
| 字体 | 系统字体栈 + mono 仅用于数字/地址/指纹/验证码 | ✅ |
| 动效 | 150–250ms;重连循环 ≥1s;`prefers-reduced-motion` 降级 | ✅ |
| 无障碍 | aria 57 处;`role=alert/dialog/switch/menu`;focus-visible 焦点环;对比度 ≥4.5:1(accent-ink 设计保证) | ✅ |

### §8 文案(⚠️ 部分)

| PRD 项 | 设计实现 | 结论 |
|---|---|---|
| 中文文案 | 全部界面中文化,风格符合 §8.2(动词开头、无黑话、错误给行动) | ✅ |
| 统一错误文案表 | 锁定/限流/会话过期/指纹变化文案齐;凭据错误统一不区分类型 | ✅ |
| **英文同步** | 设置页有 English 选项,但**界面文案无 en 版本**(§3-G3) | ⚠️ |

## 3. 缺口与差异清单

| 编号 | 类型 | 内容 | 建议处置 |
|---|---|---|---|
| G1 | 缺口 | 实例**列表无骨架屏/加载态**(仅连接中转圈) | **已决(2026-09-15):本期实现**;归入开发任务 T3(列表骨架屏) |
| G2 | 差异 | PRD §4.5 提出的"密码+验证码**同屏一次提交**"自适应路径未实现,采用通用分步(password→otp→backup) | **已决(2026-09-15)**:采纳分步;新增「已存密码直达 OTP 步骤」行为规则(密码静默提交、统一 401 回退密码屏、失败不自动清除已存密码);同屏输入明确不做。PRD §4.5 / 设计文档 §5.3 已同步 |
| G3 | 缺口 | **无英文界面文案**(PRD §8.1 要求兼顾双语) | **已决(2026-09-15):本期实现**;归入开发任务 T11(完整 zh/en 双语落地) |
| D1 | 布局差异 | 主界面用**表格**(信息密度高)替代 PRD 布局图的**列表行** | **已接受(2026-09-15)**;PRD §4.1 已回写 |
| D2 | 状态差异 | `locked` 独立为第七态(红),PRD 中是"需登录"子态(琥珀) | **已接受(2026-09-15)**;PRD §6.1 已回写 |
| D3 | 范围差异 | 内嵌 dsh 界面为占位渲染;凭据/指纹/审计为示例数据;钥匙链/代理/端口设置为静态展示 | 交付说明已声明为一期限定,合理 ✅ |

## 4. 总评

- **覆盖率**:PRD 页面 9/9、浮层 5/5、流程 4/4、状态 7/7 全部有实现与演示触发;
- **质量**:结构零残留、脚本语法通过、无障碍与动效规范落实、token 体系与 brand-spec 完全同源;
- **已决项**:G1(骨架屏 → T3)、G2(分步认证 + 已存密码直达 OTP)、G3(英文文案 → T11)、D1(表格布局)、D2(locked 独立第七态)已全部确认并回写 PRD / 设计文档 / 开发任务清单。